# Assets

Server-stack mark in Server Hub branding (abyss #121316, panel #1b1e22 units
with moss/ember/brick status LEDs),
generated by `splash-gen2.ps1` (see Temp\opencode dir at generation time):

- `icon.png` — 1024×1024 app icon (wired as `expo.icon`)
- `adaptive-icon.png` — 1024×1024 Android adaptive icon foreground, mark at
  82% for the safe zone (wired as `android.adaptiveIcon.foregroundImage`)
- `splash.png` — 1024×1024 splash mark, shown at 200dp centered on #121316
  (wired via the `expo-splash-screen` plugin + legacy `splash` block)

Still TODO:
- `notification-icon.png` — 96×96 monochrome white notification icon (add back to app.json expo-notifications plugin config when ready)

Color reference from Server Hub design tokens:
- Background: #121316 (abyss)
- Accent: #f59e0b (ember)
