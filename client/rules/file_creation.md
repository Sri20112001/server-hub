# File Creation Rules

Before creating new files:

* check if existing files should be extended instead
* avoid unnecessary file fragmentation
* avoid creating abstractions too early
* group closely related logic together initially

Create new files only when:

* responsibility becomes clearly separate
* file size becomes difficult to maintain
* reuse becomes meaningful
